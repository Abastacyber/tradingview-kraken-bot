services:
  - type: web
    name: tradingview-phemex-bot
    env: python
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT -c gunicorn.conf.py 
